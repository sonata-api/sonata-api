{
  "name": "sonata-api-monorepo",
  "private": true,
  "scripts": {
    "install:all": "npx lerna exec -- \"npm i\"",
    "build:all": "npx lerna exec -- \"npm run build\""
  },
  "devDependencies": {
    "lerna": "^7.1.5",
    "typescript": "^5.1.6"
  }
}

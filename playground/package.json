{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "pnpm build && (cd dist && NODE_PATH=../node_modules node index.js)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sonata-api": "file:../packages/sonata-api"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
